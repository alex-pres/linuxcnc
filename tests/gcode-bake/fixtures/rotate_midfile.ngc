(rotate_midfile.ngc - the XY rotation changes AFTER motion has begun.)
(This is the one program where the new accumulated extents are expected to)
(differ from the retained oracle, and the difference is the point:)
(unrotate_preview rotates every point - including the ones emitted under)
(R0 - by the FINAL rotation, so a point laid down at 0 degrees is)
(un-rotated by 40. Accumulating during the fill applies each move's own)
(rotation, which is the coherent answer. See design.md, "Extents)
(accumulate during the fill", and test_extents_oracle.py.)
G20 G90 G94
G10 L2 P1 X0 Y0 R0
G54
G0 X0 Y0 Z0
G1 F20 X1 Y0
G1 X1 Y1

G10 L2 P1 X0 Y0 R40
G54
G1 X2 Y1
G1 X2 Y2
G1 X0 Y2
G0 Z0.5
M2
